services:
  - type: web
    name: tinder-clone
    runtime: node
    buildCommand: cd web && npm install && npm run build && cd ../mock-server && npm install
    startCommand: cd mock-server && node server.js
    envVars:
      - key: NODE_ENV
        value: production
